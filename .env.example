### Local environment variables example
# Copy this file to `.env` and fill in your real key locally (DO NOT commit `.env`).

# Groq / Grok API key (example variable name used by the application)
GROQ_API_KEY=
